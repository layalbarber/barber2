import { CustomTimeAgoPipe } from './cutom-time-ago.pipe';

describe('CutomTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
